version: '3.6'

services:

  logspout_agent:
    image: gliderlabs/logspout:v3.2.8
    command: syslog://<%= @config['logagent']['syslog_host'] %>:5000
    environment:
      - RETRY_COUNT=<%= @config['logagent']['retry_count'] %>
    volumes:
      - /var/run/docker.sock:/tmp/docker.sock
